# 项目领域上下文

本文只定义领域词汇、边界和不变量，不记录 Android、JSUI、CXR 或具体模型的实现细节。

## 核心词汇

### 用户

盲人或低视力用户。默认用户不依赖手机屏幕完成主流程，并且正在行走或持有盲杖时只能进行低负担的实体键和语音操作。

### 会话（Session）

从用户发起一次任务开始，到任务完成、取消或失败结束的一段连续上下文。导航、入口观察、菜单识别和后续追问必须共享同一个 `session_id`。

### 导航事件（Navigation Event）

由手机地图和定位模块产生的结构化事实，例如开始导航、接近转弯、偏航、重新规划、GPS 信号弱、到达和停止。导航事件不包含每个 GPS 采样点。

### 系统提醒（System Reminder）

由确定性规则根据导航或设备状态产生的简短语音提示，例如“前方右转”或“即将到达，请按键观察入口”。系统提醒不等于自动拍摄。

### 观察请求（Observation Request）

请求视觉模块分析一帧或一个短时窗口。第一阶段只接受用户实体键触发或系统提醒后的用户确认；未来才增加预授权的主动策略。

### 观察结果（Observation Result）

视觉模型、OCR 或规则模块返回的结构化事实，包含任务类型、摘要、风险、文字、实体、置信度和是否需要重拍。它不是安全保证，也不直接控制设备。

### 主动观察（Proactive Observation）

系统在用户预先授权的模式中，根据任务状态和低频事件请求观察。第一阶段关闭，不能与系统提醒混为一谈。

### 设备传输（Device Transport）

手机和眼镜之间的通信接缝。实现可以是 Rokid CXR/系统通道、未来的直连协议或测试模拟器；业务层不能依赖某一个实现。

### 可见表情辅助（Visible Expression Assistance）

用户明确请求后，对单帧中可见的粗粒度面部表情进行描述，并明确说明不确定性。不识别身份，不推断真实情绪或心理状态。

## 不变量

1. 手机屏幕不是盲人主流程的必经步骤。
2. Agent 不能直接调用摄像头、地图或播报；它只能产生结构化建议。
3. 播报由统一的语音队列按优先级仲裁。
4. 地图路线信息和视觉现场信息分开记录；冲突时优先提醒眼前疑似风险，但不擅自修改长期路线。
5. 低置信度结果必须允许重拍或明确告知无法判断。
6. 研究证据、设备 dump、APK 和密钥不能进入生产源码目录。
