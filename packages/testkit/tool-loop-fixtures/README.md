# Tool Loop 夹具

这里保存 ToolCall、ToolResult、授权拒绝、超时、重复调用和路口观察的离线夹具。

夹具必须可以在不连接真机、地图网络或云端模型的情况下回放完整 Loop，并验证：

- 副作用 Tool 的幂等键；
- 未授权观察被拒绝；
- 过期/低置信度事实只能生成等待或重查；
- Tool 失败进入 `ContractError`；
- 回放不再次调用实时模型。
