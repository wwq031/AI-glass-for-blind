{
  "name": "ai-glass-for-blind",
  "version": "0.1.0",
  "private": true,
  "description": "乐奇 AI 眼镜 - 助盲导航最小纵向切片(手机端 × 高德地图)",
  "type": "module",
  "scripts": {
    "dev": "node apps/phone-companion/src/navigation/demo.ts",
    "test": "node --test tests/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "validate:contracts": "node tools/validate-contracts.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "typescript": "^5.8.0"
  }
}
