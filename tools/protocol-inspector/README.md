# Protocol Inspector（预留）

这里预留 CXR、蓝牙、局域网和模拟传输的诊断工具位置。当前阶段只建立接口和日志约定，不实现协议劫持或刷机工具。

## 目标

- 记录连接、认证、请求、响应和断开事件
- 对原始载荷做脱敏、哈希和可重放标记
- 将观测结果转换为研究材料，而不是直接耦合业务逻辑

## 安全要求

不得记录账号令牌、私钥、原始个人图像或未经授权的第三方通信内容。生产运行时默认关闭原始载荷采集。
